services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.demo
    container_name: securevault-demo
    ports:
      - "3000:3000"
    volumes:
      - securevault-demo-data:/app/data
      - securevault-demo-uploads:/app/uploads
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_URL=/app/data/securevault.db
      - STORAGE_PATH=/app/uploads
      - LOG_LEVEL=info
      - DEMO_MODE=true
      - DEMO_USERNAME=demo_admin
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget -qO- http://127.0.0.1:3000/api/health >/dev/null || exit 1"
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s

volumes:
  securevault-demo-data:
    driver: local
  securevault-demo-uploads:
    driver: local
